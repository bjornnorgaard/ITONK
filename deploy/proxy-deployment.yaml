apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: proxy
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: proxy
    spec:
      containers:
      - name: proxy
        image: itonk/proxy:v1
        ports:
        - containerPort: 80

